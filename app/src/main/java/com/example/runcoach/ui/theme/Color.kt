package com.example.runcoach.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Scheme Colors
val DarkBackground = Color(0xFF0F0F10)
val DarkSurface = Color(0xFF1C1C1E)
val DarkPrimary = Color(0xFFA3E635)       // Neon Lime Green
val DarkSecondary = Color(0xFF818CF8)     // Soft Indigo
val DarkTertiary = Color(0xFFFB923C)      // Warm Orange
val DarkOnBackground = Color(0xFFF3F4F6)
val DarkOnSurface = Color(0xFFE5E7EB)

// Light Scheme Colors
val LightBackground = Color(0xFFF9FAFB)
val LightSurface = Color(0xFFFFFFFF)
val LightPrimary = Color(0xFF4F46E5)      // Sleek Indigo
val LightSecondary = Color(0xFFF97316)    // Vivid Energy Orange
val LightTertiary = Color(0xFF10B981)     // Emerald Green
val LightOnBackground = Color(0xFF111827)
val LightOnSurface = Color(0xFF1F2937)

// Status / Workout type colors
val ColorCompleted = Color(0xFF10B981)    // Green — completed workout
val ColorWarning   = Color(0xFFEF4444)    // Red — warning / skipped
val ColorRest      = Color(0xFF9CA3AF)    // Gray — rest day

// Workout type accent colors
val ColorEasy      = Color(0xFF22C55E)    // Green — easy run
val ColorLong      = Color(0xFF6366F1)    // Indigo — long run
val ColorTempo     = Color(0xFFF97316)    // Orange — tempo run
val ColorRecovery  = Color(0xFF06B6D4)    // Cyan — recovery / CT
val ColorRace      = Color(0xFFFBBF24)    // Amber — race day
val ColorSkipped   = Color(0xFF6B7280)    // Mid-gray — skipped