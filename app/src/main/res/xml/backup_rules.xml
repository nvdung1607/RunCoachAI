<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include all database files (Room database) -->
    <include domain="database" path="." />
    
    <!-- Include all file-based assets (DataStore preferences, stored in files/) -->
    <include domain="file" path="." />
    
    <!-- Include standard shared preferences -->
    <include domain="sharedpref" path="." />
</full-backup-content>